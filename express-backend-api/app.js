const express = require('express')
  //  sql = require(sql)
  require('dotenv/config');

const app = express();

// sql.connect(
//   process.env.DB_CONNECTION,
//   { useNewUrlParser: true}
// );

app.listen(3001);